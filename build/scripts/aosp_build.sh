#!/bin/sh

echo "Building AOSP ..."

# Set up the environment
# . build/envsetup.sh

# Choose a target to build
# lunch aosp_x86_64-eng

# Build the code
# USE_CCACHE=1 CCACHE_DIR=ccache m -j (($(nproc) - 2))
